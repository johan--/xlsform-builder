<% if target.errors.any? %>
  <div class="panel panel-danger">
    <div class="panel-heading">Invalid <%= target.class.human_name %></div>
    <div class="panel-body">
      <ul>
        <% FormBuilder::Errors.new(target.errors).each do |error| %>
          <li><%= error %></li>
        <% end %>
      <ul>
    </div>
  </div>
<% end %>
